# Dependencies
node_modules/

# Build outputs
dist/
build/
types/

# Package manager
pnpm-lock.yaml

# Python
__pycache__/
*.pyc
.venv/
.uv/
uv.lock

# Tool caches. These stay out of git only because each tool writes its own
# .gitignore inside the directory. Prettier does not read those, so a working
# copy that has run the Python suite fails `format:check` on .pytest_cache/README.md.
.pytest_cache/
.ruff_cache/

# Generated: the self-contained CLI bundled into the Python wheel at build time.
python/agent_sanitizer/_bundled/

# Template sync
.template-version

# Coverage
coverage/

# Stryker mutation testing (generated)
.stryker-tmp/
reports/mutation/

# Release sections are written by scripts/promote-changelog.mjs — keep the exact
# ASCII `## [x.y.z] - date` formatting; don't let a formatter rewrite it.
CHANGELOG.md

# Generated: the invisible-charset SSOT is written by scripts/gen-invisible-charset.mjs
# with JSON.stringify (one code point per line); prettier would repack the array
# and fight the generator on every regeneration. The generator owns its format.
python/agent_sanitizer/data/invisible-charset.json

# Generated: the same generator's JS-side output. A flat number array is the one
# shape Prettier repacks by `fill`, so a formatted copy and a freshly generated
# one differ by whitespace alone — the committed module had already drifted from
# its own generator that way, invisible to the SSOT round-trip in
# test/invisible-charset.test.mjs because that test compares code points, not
# bytes. The generator emits one code point per line and owns the format.
src/cf-charset.mjs
