{
  "$comment": "SSOT source -> cheap guard test(s). When a listed source is staged, .hooks/run-ssot-guards.mjs runs the paired `node --test` files and blocks the commit on failure \u2014 so a contract test can never silently break on main because its data changed without it (this bit twice: the release-token guard on auto-version.yaml, and the instructions guard tests). Keep every paired test fast (~1s); test/ssot-guard-pairs.test.mjs asserts all paths here exist.",
  "pairs": {
    ".github/workflows/auto-version.yaml": ["scripts/version-bump.test.mjs"],
    ".github/scripts/version-bump.sh": ["scripts/version-bump.test.mjs"],
    ".github/mutation-shards.json": ["test/mutation-shards.test.mjs"],
    "src/invisible.mjs": [
      "test/invisible-charset.test.mjs",
      "test/instructions.test.mjs"
    ],
    "src/instructions.mjs": ["test/instructions.test.mjs"],
    "python/agent_sanitizer/secrets/data/credential-names.json": [
      "test/credential-names-export.test.mjs"
    ],
    "config/review-severities.json": [".github/scripts/post-pr-review.test.mjs"]
    "package.json": ["test/claude-hooks-exports.test.mjs"]
  }
}
